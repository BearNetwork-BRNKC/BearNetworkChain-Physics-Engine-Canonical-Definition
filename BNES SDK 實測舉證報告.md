# BNES SDK v1.3.0 實測舉證報告

> **宣告**：本 SDK 為 BearNetworkChain BNES 物理引擎之專屬網關實作，非以太坊或其任何分叉之衍生物。

---

## 舉證方法論

> 如同量測一杯水，必須明確說明：**量什麼、用什麼量、量到什麼**。  
> 本報告對每一項測試，均遵循以下三要素進行舉證：
>
> | 要素 | 說明 |
> |------|------|
> **被測物** | 系統中被驗證的那一個特定行為或不變量
> **量測工具** | 用什麼方式（輸入＋觀察方式）觀測這個行為
> **量測讀數** | 實際觀察到的輸出值，以及判定通過或失敗的標準

---

## 舉證項目

---

### 舉證 01 ── Gas Price 定價不變量

**被測物**：系統對 `eth_gasPrice` 查詢的回應值是否永遠固定在 BNES 規格所定義的 0.0005 Gwei。

**量測工具**：  
向系統發送一個標準的 Gas Price 查詢請求，取回 JSON 回應，讀取 `result` 欄位的值。

**量測讀數**：
- ✅ 系統回傳：`0x7A120`（十進位 500000，等同 0.0005 Gwei）
- ❌ 失敗條件：回傳任何非 `0x7A120` 的值，即視為定價漂移

**說明**：此值被硬化寫入系統，任何試圖修改它的人，下一次測試執行即會立刻被攔截並回報 `[RF-8 Violation]`。我們以「故意將此值改為 `0x88888`」進行了實際破壞性驗證，系統確實噴出了錯誤，恢復後再次通過。

---

### 舉證 02 ── Chain ID 決定性

**被測物**：系統的鏈識別碼（ChainID）在任意時刻查詢，是否永遠一致地回傳正確值。

**量測工具**：  
向系統查詢 `eth_chainId`，取回 JSON 回應，讀取 `result` 欄位。

**量測讀數**：
- ✅ 系統回傳：`0x9c8ce`（即十進位 641230，BearNetworkChain 主鏈唯一識別碼）
- ❌ 失敗條件：回傳任何其他值，即視為 `[RF-2 State Non-determinism]` 違規

---

### 舉證 03 ── 交易收據決定性回顯

**被測物**：系統是否能將交易雜湊原樣傳遞，並且同時保持請求 ID 的決定性一致性。

**量測工具**：  
向系統提交一個帶有特定交易雜湊 A 與請求 ID B 的收據查詢，觀察回應體是否同時包含 A 和 B。

**量測讀數**：
- ✅ 回應體確實包含輸入的交易雜湊字串
- ✅ 回應體確實包含輸入的請求 ID（數值 42）
- ❌ 失敗條件：雜湊消失或 ID 不符，視為資料流中存在語義破壞

---

### 舉證 04 ── eth_call 物理沙盒隔離

**被測物**：系統的 `eth_call` 是否正確路由至預執行沙盒（Pre-flight Sandbox），而非直接執行真實狀態轉移。

**量測工具**：  
發送一個智能合約呼叫查詢，觀察回應是否帶有沙盒識別標記而非真實狀態。

**量測讀數**：
- ✅ 系統回傳含有 BNES 沙盒標識的回應（`0x_bn_sandbox_output`）
- ❌ 失敗條件：回傳真實鏈上狀態，代表沙盒隔離破損

---

### 舉證 05 ── 高阻尼位址哨兵標記

**被測物**：Sentinel（哨兵）系統是否能自動偵測具有特定物理特徵的目標地址，並在交易流轉過程中主動標記監控旗標。

**量測工具**：  
建立一筆目標地址尾碼為 `0xFF` 的合法交易，送入系統，在交易被傳遞至下一處理層之前，擷取並讀取該交易的物理觀測標記欄位（ObservationHint）。

**量測讀數**：
- ✅ 讀取到的 ObservationHint 中，`HintPoliceWatchlist` 旗標為開啟（bit = 1）
- ❌ 失敗條件：旗標為 0，代表哨兵對高阻尼位址視而不見

**說明**：這驗證了系統不是被動的交易中繼站，而是一個主動進行物理特徵掃描的觀測引擎。

---

### 舉證 06 ── 網路高壓下的物理特徵注入

**被測物**：當 Gamma（Γ）引擎回報網路壓力處於正向趨勢時，系統是否會自動在交易的物理觀測欄位中注入高阻尼路徑旗標。

**量測工具**：  
將網路壓力觀測器設定為「趨勢值為正（+1）」，通過此觀測器送入一筆普通交易，讀取交易的 ObservationHint 欄位。

**量測讀數**：
- ✅ `HintHighDampingPath` 旗標為開啟（bit = 1）
- ❌ 失敗條件：旗標為 0，代表 Γ 引擎的壓力回饋訊號未能正確傳遞至交易處理層

---

### 舉證 07 ── 交易完整生命週期（PQC 綁定 → 提交）

**被測物**：一筆合法交易進入系統後，是否確實完整地走過「PQC 後量子密碼學簽章綁定」與「SRSG 引擎提交」兩個不可省略的關鍵步驟。

**量測工具**：  
送入一筆合法的 RLP 交易，在流程結束後，查詢 SRSG 引擎是否確實收到了「提交（Submit）」的指令。

**量測讀數**：
- ✅ SRSG 引擎確認已執行提交（`submitted = true`）
- ❌ 失敗條件：`submitted = false`，代表交易在 PQC 綁定或提交環節中途失敗

---

### 舉證 08 ── RF-ZERO 零分配守護（四種 RPC 方法）

**被測物**：系統的熱路徑（Hot Path）在執行期間，是否嚴格遵守 RF-ZERO（零分配）約束，不引入過量的動態記憶體分配。

**量測工具**：  
對四種 RPC 方法（Gas Price 查詢、Chain ID 查詢、收據查詢、發送交易），各執行 100 次，取每次呼叫的平均 heap 分配次數（allocs/op）。

**量測讀數**：
| 被測方法 | 分配次數 | 上限 | 判定 |
|----------|----------|------|------|
| 發送交易 | 8 allocs/op | 20 | ✅ |
| Gas Price | 8 allocs/op | 20 | ✅ |
| Chain ID | 8 allocs/op | 20 | ✅ |
| 收據查詢 | 8 allocs/op | 20 | ✅ |

- ❌ 失敗條件：任一方法超過 20 allocs/op，視為 `[RF-2 State Non-determinism]` 違規，即代表有人在熱路徑中引入了動態反射分配

---

### 舉證 09 ── E2E 端對端物理鏈路完整性（交叉驗證）

**被測物**：從最外層的 RPC 入口，到最內層的見證包（WitnessBundle），整個資料轉換鏈路的每個節點，是否保持資訊的完整性與一致性。

**量測工具**：  
建立一筆目標地址為 `A`、金額為 `B` 的交易。從 RPC 入口送入，依序在每個轉換節點「暫停」並讀取當前狀態，逐層比對。

**量測讀數（逐層讀數）**：

| 量測點 | 量測項目 | 預期讀數 | 判定 |
|--------|----------|----------|------|
| 第一層：CIR 生成後 | 目標地址 | 等於輸入地址 `A` | ✅ |
| 第二層：執行圖 | 節點類型清單 | 包含：PQC_VERIFY, TYPE_RESOLVE, ZK_PROVE, EVM_EXECUTE, STATE_COMMIT, GAMMA_OBSERVE | ✅ |
| 第三層：見證包 | PQC 信任根 | 長度 > 0（存在有效簽章） | ✅ |
| 第三層：見證包 | ZK 證明物件 | 長度 > 0（存在有效證明） | ✅ |

- ❌ 失敗條件：任何一層的讀數與前一層不一致，或缺少必要組件

---

### 舉證 10 ── 損毀資料的 RF-13 錯誤映射

**被測物**：當一段無效的、無法解析的十六進位資料（損毀 RLP）進入系統時，系統是否正確地識別錯誤類型，並回傳對應的 BNES 錯誤代碼。

**量測工具**：  
向系統發送交易資料為 `0xdeadbeef`（一段無效的 RLP 資料），觀察系統回應中是否包含錯誤標識。

**量測讀數**：
- ✅ 回應中包含 `[RF-13]`（Proof-Crypto Inconsistency / RLP 格式違規）
- ❌ 失敗條件：系統不回報錯誤、靜默吞噬、或回傳其他錯誤碼，均代表錯誤映射系統失效

---

### 舉證 B01 ── 極限壓力下的吞吐穩定性

**被測物**：在連續高強度壓力下（5 秒不間斷），系統每次請求的處理時間與記憶體分配是否保持穩定，不出現洩漏或衰退。

**量測工具**：  
以 Go 語言內建的效能計時器，對系統的 RPC 熱路徑持續施壓 5 秒，記錄每次呼叫的：平均耗時（ns/op）、記憶體用量（B/op）、分配次數（allocs/op）。

**量測讀數**：
| 指標 | 量測值 | 判定基準 |
|------|--------|----------|
| 總執行次數 | 5,855,998 次 | 越高代表越穩定 |
| 平均耗時 | 1,032 ns/op | 規格參考值 ≤ 2,356 ns/op ✅ |
| 記憶體分配 | 803 B/op | 無上限，僅監控趨勢 |
| **分配次數** | **8 allocs/op** | **≤ 20（RF-2 上限）✅** |

- ❌ 失敗條件：分配次數在壓力下升高（代表 GC 壓力累積），或耗時出現跳躍（代表記憶體逃逸）

---

## 破壞性驗證（證明測試不是擺設）

> 如同用磅秤量水：我們刻意在水裡加了一顆石頭，磅秤確實顯示了更重的數值，石頭移除後讀數恢復——這證明磅秤是真的有在量測的。

**操作**：將 Gas Price 回傳值從規格值 `0x7A120` 改為任意值 `0x88888`。

**系統回應**：
```
[RF-8 Violation] GasPrice 漂移: 得到 0x88888, 預期 0x7A120
--- FAIL
```

**恢復後**：將值改回 `0x7A120`，所有測試立即恢復通過。

**結論**：測試具備真實攔截效力，並非空殼驗收。

---

## 最終審計

```
10 項舉證：全部通過 ✅
1 項破壞性驗證：確認攔截有效 ✅
極限壓力 585 萬次：8 allocs/op，穩定無洩漏 ✅

BNES SDK v1.3.0 實測判決：CANONICAL PASS
```
