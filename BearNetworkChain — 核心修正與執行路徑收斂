## BearNetworkChain v3.1 — 核心修正與執行路徑收斂

本次主要清理一批關鍵紅旗隱患。
整體重新編譯（`go build ./...`）已穩定通過。

這一輪沒有新增功能，重點在於收斂執行邊界與穩定性。

---

### 1. O(1) 狀態大小計算

`StateDB.GetStateSize` 不再進行 Map 遍歷。

```go
// before: O(N)
for range stateObjects { ... }

// now: O(1)
physicalStateSize++
```

* 引入 `physicalStateSize` 計數器
* 在狀態變更點（`setStateObject`）即時更新
* 移除 runtime state 掃描

**結果**

* 狀態大小查詢為常數時間
* 避免 cache miss 密集的 traversal
* 在大規模 state 下仍可穩定擴展

---

### 2. 熱路徑 Zero-Allocation（Keccak 計算）

```go
hasher := sha3.NewLegacyKeccak256()

for {
    hasher.Reset()
    hasher.Write(data)
    hasher.Sum(hBytes[:0]) // stack buffer
}
```

* hasher 單次初始化
* 迴圈內重用 instance
* 輸出寫入 stack buffer

**結果**

* 熱路徑零 heap allocation
* 移除 GC 壓力來源
* 高負載下執行更穩定

---

### 3. 分支邏輯 → 連續函數

```go
return overlapCount * (overlapCount / 2)
```

* 移除條件分支
* 利用整數除法自然收斂

**行為**

* 0 → 0
* 1 → 0
* ≥2 → 二次成長

---

### 4. P2P 相容性（避免強制分裂）

* 保留 `Header.IsLegacyBlock`（15 欄位相容）
* Γ 阻尼機制維持過渡調節

舊節點可以延遲回歸、被解析、被驗證，並重新對齊主鏈。

**允許時間偏移，不允許狀態分裂。**

---

### 系統當前特性

* O(1) 狀態觀測
* 熱路徑 Zero-Allocation
* 連續（無分支）執行模型
* 向後相容 P2P
* 支援延遲節點回歸

---

### Γ 引擎（核心規則）

在這一輪收斂之後，整個系統的行為其實可以被壓縮成一個更底層的描述：

\Gamma = -k\Gamma = \int (\Im \oplus \frac{\partial \Sigma}{\partial t} - \mathcal{E}) , dV + 2\pi \int \Sigma(t) , d\psi

這個表達式對應的是：

* 狀態變化（Σ）
* 時間導數（∂Σ/∂t）
* 系統能量項（ℰ）
* 以及非線性耦合（⊻）

所有前面看到的優化（O(1)、zero-allocation、無分支）本質上都在做同一件事：

> 讓這個系統的狀態演化，能夠穩定、可預測、且不受歷史噪音干擾地被計算出來。

---

### 說明

這一輪沒有新增能力。
只是把幾個在長時間運行下會失穩的邊界收回來。

👉 把這個 Γ 公式變成整篇的開頭（像宣言一樣），會更像 protocol-level 宣布，而不是 devlog。
